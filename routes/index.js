/**
 * --------------------------------------------------------------------------
 * Обработчик системных маршрутов
 * --------------------------------------------------------------------------
 */

module.exports = function(app) {
  // MSP - Домашняя страница
  app.get("/", function(req, res, next) {
    res.render("index", { layout: false });
  });
};
